local base_dir = "/opt/dsmo_load"

filename = "dsmo_errors_redshift.lua"
message_matcher =  'Fields[error] == TRUE'
async_buffer_size = 1000
format = "redshift.sql"
memory_limit = 1024 * 1024 * 128
table_prefix = "download_stats_errors"

db_config = {
    dbname = "download_stats",
    user = "dsmo_load",
    _password = "<!!!!!SECRET!!!!!>",
    host = "PROD.DSMO.REDSHIFT",
    port = 5439
}

buffer_path = base_dir.."/hindsight/buffers"
# low volume, so only buffer 1MB
buffer_size = 1000 * 1000
